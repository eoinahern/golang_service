package main

import (
  "routes"
  //"database/sql"
  //_ "github.com/go-sql-driver/mysql"
)



func main() {
	
	 routes.NewRouter()
	//router.CreateHandler()
	
	 //db, err := sql.Open("mysql", "eoin:pass@/weather_app")
	 
	 /*if err != nil {
        panic(err)
	 }
	 
	 err = db.Ping()
		if err != nil {
	    panic(err.Error()) // proper error handling instead of panic in your app
	 }
		
     _, err = db.Exec("insert into city (name, latitude, longitude) values (?, ?, ?)", "dubdub",12.3333, 23.4444 )
    db.Close();*/
    
    
	
	
	
}


